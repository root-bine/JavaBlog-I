# 范例合集

## 1、获取方法：

```java
public class Demo01 {
    public static void main(String[] args) {
        Properties pt = new Properties();
        pt.setProperty("Java","125");
        pt.setProperty("Tom","15");
        pt.setProperty("Marry","25");
        Set<String> set = pt.stringPropertyNames();
        for (String key:set) {
            String value = pt.getProperty(key);
            System.out.println(key+":"+value);
        }
    }
}
```

```apl
Java:125
Tom:15
Marry:25
```

## 2、写入数据的方法：

```java
public class Demo01 {
    public static void main(String[] args) throws IOException {
        show01();//使用中文，结果可以正常显示
        show02();//使用中文，结果会出现乱码【编码格式不同】
    }

    private static void show01() throws IOException {
        Properties pt = new Properties();
        pt.setProperty("Java","125");
        pt.setProperty("Tom","15");
        pt.setProperty("Marry","25");
        FileWriter fw = new FileWriter("D:\\JavaCode\\File_code\\prp1.txt");
        pt.store(fw,"save data");
        fw.flush();
        fw.close();
    }
    private static void show02() throws IOException {
        Properties pt = new Properties();
        pt.setProperty("Java","125");
        pt.setProperty("Tom","15");
        pt.setProperty("Marry","25");
        FileOutputStream fos = new FileOutputStream("D:\\JavaCode\\File_code\\prp2.txt");
        pt.store(fos,"save Data");
        fos.close();
    }
}
```

## 3、读取数据的方法：

```java
public class Demo01 {
    public static void main(String[] args) throws IOException {
        show01();//使用中文，结果可以正常显示
        System.out.println("----------------");
        show02();//使用中文，结果会出现乱码【编码格式不同】
    }

    private static void show01() throws IOException {
        Properties pt = new Properties();
        FileReader fr = new FileReader("D:\\JavaCode\\File_code\\prp1.txt");
        pt.load(fr);
        Set<String> set = pt.stringPropertyNames();
        for (String key:set) {
            String value = pt.getProperty(key);
            System.out.println(key+"="+value);
        }
    }
    private static void show02() throws IOException {
        Properties pt = new Properties();
        FileInputStream fis = new FileInputStream("D:\\JavaCode\\File_code\\prp2.txt");
        pt.load(fis);
        Set<String> set = pt.stringPropertyNames();
        for (String key:set) {
            String value = pt.getProperty(key);
            System.out.println(key+"="+value);
        }

    }
}
```

```apl
迈克尔=125
詹姆斯=15
杜兰特=25
----------------
Java=125
Tom=15
Marry=25
```

